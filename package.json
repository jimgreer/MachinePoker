{
  "name": "machine-poker",
  "version": "0.5.1",
  "description": "The library for Machine Poker Competitions",
  "keywords": [
    "competition",
    "poker"
  ],
  "repository": "git://github.com/mdp/MachinePoker.git",
  "author": "Mark Percival <m@mdp.im> (http://markpercival.us)",
  "scripts": {
    "test": "rm -rf lib && coffee --compile -o lib/ src/ && mocha --no-colors --compilers coffee:coffee-script/register test/*_test.coffee",
    "XXprepublish": "rm -rf lib && coffee --output lib --compile src"
  },
  "devDependencies": {
    "mocha": ">=0.0.0",
    "coffee-script": ">=1.4.0"
  },
  "dependencies": {
    "async": ">=0.1.22",
    "binions": ">=0.6.4",
    "hoyle": ">=0.2.0",
    "optimist": ">=0.3.5",
    "readline-sync": "^1.4.7",
    "request": ">=2.11.4"
  },
  "main": "./lib/index.js",
  "engines": {
    "node": "0.8.x"
  },
  "license": "MIT"
}
